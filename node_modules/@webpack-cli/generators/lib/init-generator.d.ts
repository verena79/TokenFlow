import { CustomGenerator } from "./types";
/**
 *
 * Generator for initializing a webpack config
 *
 * @class 	InitGenerator
 * @extends CustomGenerator
 * @returns {Void} After execution, transforms are triggered
 *
 */
export default class InitGenerator extends CustomGenerator {
    answers: Record<string, unknown>;
    configurationPath: string;
    force: boolean;
    generationPath: string;
    packageManager: string;
    resolvedGenerationPath: string;
    supportedTemplates: string[];
    template: string;
    cli: any;
    constructor(args: any, opts: any);
    prompting(): Promise<void | any>;
    installPlugins(): Promise<void>;
    writing(): void;
    end(): void;
}
